{
    "name": "@startup-platform/config-validator",
    "version": "1.0.0",
    "description": "Shared configuration validation using Zod",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "clean": "rm -rf dist"
    },
    "devDependencies": {
        "@types/node": "^25.2.0",
        "typescript": "^5.2.2"
    },
    "dependencies": {
        "dotenv": "^16.3.1",
        "zod": "^3.22.4"
    }
}
